# Bets with Friends

## How to Run
- Copy the `.env.template` file and its contents into another file called `.env`
- Fill in all the info required in `.env`
- Run `npm start` from the root of the project